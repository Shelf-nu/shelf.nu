node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env.local

# macOS
.DS_Store

# Temporary files created by Metro
.metro-health-check*

# Maestro E2E tests
.maestro/env/test.env
.maestro/results/
.claude/

# Maestro screenshot artifacts. `takeScreenshot: <name>` writes <name>.png to
# the app root (the CWD maestro runs from), so ignore root-level PNGs too —
# legitimate image assets live under assets/ / ios/ / android/, not here.
*.screenshot.png
/*.png

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Android Play service account key (secret — never commit)
google-service-account*.json
