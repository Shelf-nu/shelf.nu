/**
 * Limits for saved reports (Advanced Reports add-on).
 *
 * @see {@link file://./service.server.ts}
 */

/** Maximum number of saved reports one workspace can keep. */
export const MAX_SAVED_REPORTS = 100;

/** Maximum length of a saved report's name. */
export const SAVED_REPORT_NAME_MAX_LENGTH = 60;
