/**
 * Constants for User Contact module
 * These constants are used to define the structure of user contact data that we will select with queries
 */
export const USER_CONTACT_SELECT = {
  id: true,
  userId: true,
  phone: true,
  street: true,
  city: true,
  stateProvince: true,
  zipPostalCode: true,
  countryRegion: true,
};
